-- DDL bootstrap script: create catalog
-- Replace <CATALOG_NAME> before execution.
--
-- Example:
--   CREATE CATALOG IF NOT EXISTS nyc_taxi_dev_jane;
--
-- If your Free Edition workspace does not allow creating catalogs,
-- skip this script and use an existing catalog in bundle variable `catalog`.

CREATE CATALOG IF NOT EXISTS <CATALOG_NAME>;
